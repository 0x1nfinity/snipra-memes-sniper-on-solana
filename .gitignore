# Dependencies
node_modules/
package-lock.json

# Environment variables (secrets)
.env
.env.local
.env.*.local

# Runtime data & state
data/
*.db
*.db-shm
*.db-wal

# Logs
*.log
npm-debug.log*
pm2.log

# Build artifacts
build/

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Platform agent dirs (local only)
.opencode/
.forge/

# Skill lock file (platform-specific, generated at install time)
skills-lock.json

# Backup files
*.bak

# Local spec document (Indonesian, gitignored)
SPEC.md
nug.mg
bug.md

# Claude Code
.claude/
.superpowers/
docs/superpowers/

# Local strategy config — personal trading params, not for the repo.
# Copy live-config.example.json / paper-config.example.json to get started;
# the bot also auto-creates these with defaults on first run.
live-config.json
paper-config.json
# Old unified config (migrated to paper-config.json + live-config.json)
config.json
# Old naming scheme (pre-v2 rename)
config.live.json
config.paper.json
# Migration backup
config.json.migrated-*.bak
# Mode marker (determined at runtime)
data/.mode
