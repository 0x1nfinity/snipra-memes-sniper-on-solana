# Dependencies
node_modules/
package-lock.json
.bak
.bak/*

# Environment variables (secrets)
.env
.env.local
.env.*.local

# Runtime data & state
data/
*.db
*.db-shm
*.db-wal

# Logs
*.log
npm-debug.log*
pm2.log

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Claude Code
.claude/
.superpowers/
docs/superpowers/

# Local strategy config — personal trading params, not for the repo.
# Copy config.live.example.json / config.paper.example.json to get started;
# the bot also auto-creates these with defaults on first run.
config.live.json
config.paper.json
# Old config (migrated to config.paper.json + config.live.json)
config.json
# Migration backup
config.json.migrated-*.bak
# Mode marker (determined at runtime)
data/.mode
